"""MCP Read — Voyage Framework MCP read-only interface foundation.

Slice 1 (FOUNDATION): package skeleton, configuration, identity, confinement,
limits, result contract, and external audit.  No server, no tools, no service
adapters, no MCP SDK import.
"""

from __future__ import annotations

MCP_READ_SCHEMA_VERSION: str = "voyage.mcp-read.v1"
